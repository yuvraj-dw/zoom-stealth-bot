{
  "name": "zoom-stealth-bot",
  "version": "1.0.0",
  "description": "Automates attending recurring Zoom webinars so you don't have to sit through them",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "1.49.0"
  }
}
